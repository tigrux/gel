
(define primes (array 2))

(for n (range 3 1000000)
    (for i primes
        (when (== (% n i) 0)
            (break)
        )
        (when (>= (* i i) n)
            (append primes n)
            (break)
        )
    )
)

(print "Calculated " (len primes) " primes")

(quit)

