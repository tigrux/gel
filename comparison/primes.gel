
(define sieve (array 2))

(for n (range 3 1000000)
    (for i sieve
        (if (== (% n i) 0)
            (break)
        )
        (if (>= (* i i) n)
            (append sieve n)
            (break)
        )
    )
)

(print "Calculated " (len sieve) " primes")

(quit)

