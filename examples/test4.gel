
(def (draw-polygon circumference vertices)
    (def side (/ circumference vertices))
    (def angle (/ 360 vertices))
    (let iterate ( (i 0) )
        (if (< i vertices)
            (begin
                (print "move " side)
                (print "turn " angle)
                (iterate (+ i 1))
            )
        )
    )
)

(draw-polygon 300 3)


(def (generate-primes)
    (def primes (array))
    (def candidate 2)

    (def (none-prime predicate)
        (= (find primes predicate) -1)
    )

    (def (can-divide-candidate i)
        (= (% candidate i) 0)
    )

    (=> ()
        (def calculating TRUE)
        (while calculating
            (if (none-prime can-divide-candidate)
                (begin
                    (array-append! primes candidate)
                    (def calculated-prime candidate)
                    (set! calculating FALSE)
                )
            )
            (set! candidate (+ candidate 1))
        )
        calculated-prime
    )
)

(def next-prime (generate-primes))

(for i (range 1 12)
    (print "Primes(" i ") = " (next-prime))
)


(quit)

