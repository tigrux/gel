
(define ls (array "red" "green" "blue"))

(define (reference-to-ls)
    (get& ls)
)

(append (reference-to-ls) "white")

(display ls)


(define message "hello")

(define (salutation)
    (define message "hallo")
    (display message)
)

(salutation)


(quit)

